default "echo"

desc "Find moon's path"
task test: =>
  "MOON_PATH: " .. where "moon" -- results returned from a task are printed to screen

desc "List some stuff"
task dir: "test", =>
  cd, status = echo "%CD%" -- calls to any unknown global will call to your operating system and return the value as a string and the status code
  print "Status: #{status}"
  "PWD: " .. cd

desc "Echo some useless information"
task echo: {"test", "dir"}, =>
  echo "doing echo"
